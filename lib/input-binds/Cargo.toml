[package]
name = "input-binds"
version = "0.1.0"
edition = "2021"

[dependencies]
base = { path = "../base" }
pool = { path = "../pool" }
hiarc = { path = "../hiarc", features = ["enable_winit"] }
serde = { version = "1.0.217", features = ["derive"] }
winit = { version = "0.30.7", default-features = false, features = ["serde"] }

[dev-dependencies]
serde_json = "1.0.134"
